import SwiftUI

struct UAE: View {
    var body: some View {
        HStack(spacing: 0) {
            Color.red
                .frame(width: 200)
            VStack(spacing: 0) {
                Color.green
                Color.white
                Color.black
            }
        }
        .ignoresSafeArea()
    }
}

#Preview {
    UAE()
}
