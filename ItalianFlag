import SwiftUI

struct ContentView: View {
    var body: some View {
        HStack {
            Color.green
            Color.white
            Color.red
        }
        .ignoresSafeArea()
    }
}

#Preview {
    ContentView()
}
